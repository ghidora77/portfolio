import { LoadContext, Plugin, OptionValidationContext } from '@docusaurus/types';

interface PluginOptions {
    mode: string;
}
type Options = Partial<PluginOptions>;
declare function vercelAnalytics(_: LoadContext, options: PluginOptions): Promise<Plugin>;
declare function validateOptions({ options, validate, }: OptionValidationContext<Options, PluginOptions>): PluginOptions;

export { Options, PluginOptions, vercelAnalytics as default, validateOptions };
